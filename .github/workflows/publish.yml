name: .NET Core

on:
  push:
    branches: [ release ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.301
    - name: Install dependencies
      run: dotnet restore src/LSHDotNet/
    - name: Build
      run: dotnet build --configuration Release --no-restore src/LSHDotNet/
    - name: Test 
      run: dotnet test --no-restore --verbosity normal src/LSHDotNet/
    - name: Publish to Nuget
      id: publish_nuget
      uses: rohith/publish-nuget@v2
      with:
        # Filepath of the project to be packaged, relative to root of repository
        PROJECT_FILE_PATH: src\LSHDotNet\LSHDotNet.csproj

        # NuGet package id, used for version detection & defaults to project name
        PACKAGE_NAME: LSHDotNet

        # Filepath with version info, relative to root of repository & defaults to PROJECT_FILE_PATH
        # VERSION_FILE_PATH: Directory.Build.props

        # Regex pattern to extract version info in a capturing group
        # VERSION_REGEX: ^\s*<Version>(.*)<\/Version>\s*$

        # Format of the git tag, [*] gets replaced with actual version
        TAG_FORMAT: v*

        # API key to authenticate with NuGet server
        NUGET_KEY: ${{secrets.NUGET_KEY}}
